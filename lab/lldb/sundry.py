# 杂货铺

# def __lldb_init_module(debugger: lldb.SBDebugger, internal_dict: dict):
    # for key, value in internal_dict.items():
    #     print(f"{key}: {type(value)}")

def java_call(debugger, command: str, result, internal_dict: dict):
    """"""
